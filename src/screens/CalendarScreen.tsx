import Text from "@/ui-kit/Text";
import { ScrollView } from "react-native";

const CalendarScreen = () => (
<ScrollView >
    <Text variant="primary">CalendarScreen</Text>
  </ScrollView>
);

export default CalendarScreen;